{
  "name": "imprintgenius-webapp",
  "version": "1.0.0",
  "description": "client facing dashboard for oder information",
  "main": "index.js",
  "engines": {
    "node": "15.3.0",
    "npm": "7.0.14"
  },
  "scripts": {
    "start": "node backend/index.js",
    "backend": "nodemon server/index.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm install --prefix backend && npm run build --prefix frontend"
  }
}
