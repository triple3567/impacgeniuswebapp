import React from "react"

const SignInError = (props) => {

    return(
        <div className="Error-Message"> 
            <text> The credentials you entered are invalid! Please try again. </text>
        </div>
    )
}
export default SignInError;